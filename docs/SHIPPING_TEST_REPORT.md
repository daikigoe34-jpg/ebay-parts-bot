# SpeedPAK Economy 検証記録

確認日：2026-09-08。計算・UIの対象：`c4c2b4d6920e0c9dcea0cb08c5fd54f95b7036b0`。

- `node --test tests/shipping.test.cjs`：14件合格。料金区分、容積重量、寸法／申告額の上限、燃油込み送料、通関費分離、未入力、端数処理を確認。
- `node --test tests/*.test.cjs`：37件合格、失敗0。既存の保存・通信断・データ鮮度・利益判定を含む。
- JavaScript構文と`git diff --check`：合格。
- 実HTMLとJavaScriptをjsdomで起動：重量・寸法の即時保存、再読み込み、手入力と自動見積の切替、元の送料保持を確認。
- レビューで見つかった手入力通関費の重複は、別の保存項目に分けて修正。旧手入力245円から自動へ切り替えても245円を二重計上しないことと、追加75円の独立保存をRED/GREENで確認。修正後のレビューは承認。
- Sites向け画面：キーなし送料計算、即時保存、再開、空欄で未確定への戻し、JSON復元、調査結果の取得先、キー未登録表示の起動確認に合格。HTMLとオフライン用ファイル参照の存在を確認。

本番のeBay API・OC API・実DDP見積、iPhone実機は未検証。Sitesの公開成功は配信状態の確認であり、API接続成功を示さない。料金表は2026-03-25発効版の参考値で、毎日最新料金を検証する機能ではない。
